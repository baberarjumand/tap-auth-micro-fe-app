import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tap-auth-micro-fe-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
